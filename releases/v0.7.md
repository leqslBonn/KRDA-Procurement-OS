---
id: release-v0-7
title: v0.7 — Memory System & Architecture
type: release-note
status: released
date: 2026-06-25
---

# v0.7 — Memory System & Architecture

> The organization's recall layer and the rules that route information.

**Status:** released · **Prev:** [v0.6](v0.6.md) · **Next:** [v0.8](v0.8.md)

## Release Note
- Added 8 memory registers and an auto-routing **memory architecture** (classify →
  store in one home → index → retrieve), with deduplication and conflict rules.

## Folder Structure
- `/memory`: `architecture.md` + 8 registers — supplier_database, commodity_database,
  rfq_database, cost_database, contract_database, project_history (index);
  meeting_history, risk_database (native). README updated.

## Agents
- — unchanged.

## Workflows
- — unchanged.

## Memory
- Index registers link to canonical records (no copying); native registers own the
  decision log and risk register. ID scheme: SUP-/CMD-/RFQ-/CD-/CTR-/PRJ-/RSK-.
- Retrieval: by ID / register / cross-link / grep. Single source of truth enforced.

## Commands
- — none yet.
